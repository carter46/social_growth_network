<IfModule mod_rewrite.c>
    RewriteEngine On

    # Hostinger shared hosting keeps document root at public_html.
    # Route all requests into Laravel's public/ folder.
    RewriteRule ^public/ - [L]
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
